#include <cmath>
#include <fstream>
#include <iostream>

using namespace std;

void path

int main() {
  
  const int N = 100000;
  const double d = 1;
  const int Gn = 100;
  const double v0 = 0.01;
  const double v1 = 1;
  const double Ra = 0.01;
  const double Rs = 0.3;
  double lambda0 = v0 / (Ra + Rs);
  double lambda1 = v1 / (Ra + Rs);
  
  return 0;
}